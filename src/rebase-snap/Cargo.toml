[package]
name = "rebase-snap"
version = "1.3.0"
authors = ["Amazon Firecracker team <firecracker-devel@amazon.com>"]
edition = "2021"
build = "../../build.rs"
license = "Apache-2.0"

[dependencies]
libc = "0.2.141"

utils = { path = "../utils" }
